import * as CurrencyApi from './currencyApi';

it('fetchCurrencyExchangeRateToPLN', async () => {
    // const eur = await CurrencyApi.fetchCurrencyExchangeRateToPLN('EUR');
    // const usd = await CurrencyApi.fetchCurrencyExchangeRateToPLN('USD');
    // const pln = await CurrencyApi.fetchCurrencyExchangeRateToPLN('PLN');
    // console.log('exchangeData eur', eur);
    // console.log('exchangeData usd', usd);
    // console.log('exchangeData pln', pln);

    // console.log('eurToPln_100', CurrencyApi.convertToPLN(100, eur))
    // console.log('plnToPln_100', CurrencyApi.convertToPLN(100, pln))
    // console.log('eurToUsd_50', CurrencyApi.convert(50, eur, usd))
    // console.log('plnToPln_20', CurrencyApi.convert(20, pln, pln))
    // console.log('eurToEur_35', CurrencyApi.convert(35, eur, eur))

    // expect(Helpers.emptyObjectToUndefined(1)).toEqual(undefined);
    // expect(Helpers.emptyObjectToUndefined(undefined)).toEqual(undefined);
    // expect(Helpers.emptyObjectToUndefined(null)).toEqual(undefined);
    // expect(Helpers.emptyObjectToUndefined({})).toEqual(undefined);
    // expect(Helpers.emptyObjectToUndefined({ a: 1 })).not.toEqual(undefined);
    // expect(Helpers.emptyObjectToUndefined({ a: 1 })).toEqual({ a: 1 });
});